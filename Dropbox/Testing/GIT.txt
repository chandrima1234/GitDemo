git branch -r = remote branch
git branch -vv = local last message and branch
git branch -d branchname  = delete branch
delete the commit from local >> git reset --soft HEAD^ (file back to stage)
delete from stage >> untrack file (back to workspace) > git reset --mixed HEAD^ 
delete from ws > git reset --hard HEAD^ (instead for head use commit id
0
Delete from remote (after pushing) >>delete file from local > commit 
then git push -f origin master (force update and it will be deleted from remote)
git push -d branch name (delete the branch from remote)
stash >temp memory (before taking pull , we can move our code to stash > then pull the latest code and pop code from memory)
git stash
got stash pop (code added ) >> Wip  
